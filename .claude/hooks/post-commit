#!/bin/bash
#
# Git Hook: post-commit
# Auto-update CLAUDE.md après chaque commit
# NS2PO Context Engineering v1.0
#

# Configuration
HOOK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$HOOK_DIR/../.." && pwd)"
UPDATER_SCRIPT="$HOOK_DIR/claude_context_updater.py"

# Logging function
log() {
    echo "[post-commit] $1" >&2
}

# Main execution
main() {
    log "🔄 Mise à jour automatique CLAUDE.md..."

    # Vérifier que le script updater existe
    if [[ ! -f "$UPDATER_SCRIPT" ]]; then
        log "⚠️ Script updater non trouvé: $UPDATER_SCRIPT"
        exit 0  # Ne pas bloquer le commit
    fi

    # Vérifier que CLAUDE.md existe
    if [[ ! -f "$PROJECT_ROOT/CLAUDE.md" ]]; then
        log "⚠️ CLAUDE.md non trouvé dans: $PROJECT_ROOT"
        exit 0  # Ne pas bloquer le commit
    fi

    # Exécuter le script de mise à jour
    cd "$PROJECT_ROOT"
    python3 "$UPDATER_SCRIPT"

    # Si CLAUDE.md a été modifié, l'ajouter au prochain commit
    if git diff --quiet CLAUDE.md; then
        log "ℹ️ CLAUDE.md déjà à jour"
    else
        log "✅ CLAUDE.md mis à jour automatiquement"
        git add CLAUDE.md

        # Créer un commit automatique pour la mise à jour
        git commit -m "docs(auto): update CLAUDE.md context engineering

🤖 Auto-generated context update:
- Migration status refresh
- Infrastructure health check
- Timeline and commands update

Generated by: claude_context_updater.py"

        log "📝 Commit automatique créé pour CLAUDE.md"
    fi
}

# Exécution avec gestion d'erreur
if main; then
    log "🎯 Context engineering terminé avec succès"
else
    log "⚠️ Erreur lors de la mise à jour (non bloquante)"
fi

exit 0  # Toujours retourner succès pour ne pas bloquer git